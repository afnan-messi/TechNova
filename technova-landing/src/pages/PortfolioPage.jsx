import { useState } from 'react'
import { FaArrowRight, FaCheck } from 'react-icons/fa'
import { AnimatePresence, motion } from 'framer-motion'
import { portfolioItems } from '../data/portfolioItems'
import './PortfolioPage.css'

const filters = ['All', 'Web', 'Mobile', 'UI/UX']
const featured = { title: 'FinTrack — Personal Finance App', description: 'FinTrack helps people build a clearer relationship with their money through simple daily actions, useful context, and an interface that never gets in the way. We shaped the product from research through launch, pairing a confident visual system with a fast, resilient mobile experience.', image: 'https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?w=1400&q=80', results: ['40% increase in weekly engagement', '60% faster onboarding completion', '4.9/5 average app rating'] }

function PortfolioPage() {
  const [activeFilter, setActiveFilter] = useState('All')
  const visibleItems = activeFilter === 'All' ? portfolioItems : portfolioItems.filter((item) => item.category === activeFilter)

  return <main className="portfolio-page"><section className="portfolio-page-hero"><div className="portfolio-page-orb portfolio-page-orb-blue"></div><div className="portfolio-page-orb portfolio-page-orb-orange"></div><motion.div className="container portfolio-page-hero-content" initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7 }}><p className="eyebrow">Our work</p><h1>Projects we're <span>proud of.</span></h1><p>Digital products made with care, clarity, and a sharp eye for the moments that matter.</p><div className="portfolio-filters" role="tablist" aria-label="Filter portfolio projects">{filters.map((filter) => <button className={activeFilter === filter ? 'portfolio-filter portfolio-filter-active' : 'portfolio-filter'} key={filter} type="button" role="tab" aria-selected={activeFilter === filter} onClick={() => setActiveFilter(filter)}>{filter}</button>)}</div></motion.div></section><section className="portfolio-featured"><div className="container"><motion.article className="portfolio-spotlight" initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}><div className="portfolio-spotlight-image"><img src={featured.image} alt={`${featured.title} project showcase`} /></div><div className="portfolio-spotlight-copy"><p className="eyebrow">Featured project / 2025</p><h2>{featured.title}</h2><p>{featured.description}</p><ul>{featured.results.map((result) => <li key={result}><FaCheck />{result}</li>)}</ul><a className="button button-small" href="#contact">View Case Study <FaArrowRight /></a></div></motion.article></div></section><section className="portfolio-grid-section"><div className="container"><div className="portfolio-grid-heading"><p className="eyebrow">Selected projects</p><h2>Work that earns its place.</h2></div><motion.div className="portfolio-page-grid" layout>{<AnimatePresence mode="popLayout">{visibleItems.map((item, index) => <motion.article className="portfolio-page-card" key={item.id} layout initial={{ opacity: 0, y: 22 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, scale: .96 }} transition={{ duration: .35, delay: index * .05 }}><a href={item.link} className="portfolio-card-link" aria-label={`View ${item.title} project`}><div className="portfolio-page-card-media"><img src={item.image} alt={`${item.title} project showcase`} loading="lazy" /><div className="portfolio-card-overlay"><span>{item.category}</span><strong>View Project <FaArrowRight /></strong></div></div><div className="portfolio-page-card-copy"><div className="portfolio-card-meta"><span>{item.category}</span><time>{item.year}</time></div><h3>{item.title}</h3><p>{item.shortDescription}</p><div className="portfolio-tags">{item.tags.map((tag) => <span key={tag}>{tag}</span>)}</div></div></a></motion.article>)}</AnimatePresence>}</motion.div></div></section><section className="portfolio-page-cta"><div className="container"><p className="eyebrow">Let’s make your next case study</p><h2>Have a project in mind?</h2><p>Bring us the challenge. We’ll bring a clear plan and a team ready to make it real.</p><a className="button button-light" href="/#contact">Start a conversation <FaArrowRight /></a></div></section></main>
}

export default PortfolioPage
